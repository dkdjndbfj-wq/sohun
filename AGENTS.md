# sohun 仓库指引

## 工作约定

- 默认用中文简洁沟通，完成请求内的修改和必要验证；常规实现细节自行判断。需要用户决定且无法从代码或上下文确认时，先推进不依赖该决定的工作。
- 用户当前要求优先于这里的项目约定和技能建议；仍遵守运行环境的权限。已获授权的工作继续执行，涉及尚未授权的发布或外部写入时，先准备可审阅的结果。
- 独立读取和检查可以并行；具备工具且宿主允许时，将有明确交付物、不会冲突的独立子任务交给子代理。简单修改直接完成。
- 旧设计、TRAE 任务书和 `docs/superpowers/` 中的执行步骤是历史参考。使用其中的业务信息前核对当前代码，不继承旧插件依赖、审批流程或逐任务提交要求。

## 源码与环境

| 位置 | 用途 |
| --- | --- |
| `电脑软件/lib`、`电脑软件/test` | Flutter 客户端及测试；桌面与 Android 共用工程 |
| `电脑软件/lib/main_mobile.dart`、`电脑软件/android` | 当前原生 Android 入口与 NFC 实现 |
| `电脑软件/community_server` | Node.js 社区 API 与个人库存同步 |
| `官网网页制作` | Node.js 官网及测试 |
| `手机软件` | 历史浏览器原型；只在任务涉及它时修改 |
| `scripts`、`.github/workflows` | 本地验收、CI 与发布流程 |

工具链版本见 CI：Flutter 3.44.4、Node.js 24。先复用现有依赖和构建脚本。
Windows 路径含中文，原生构建用 `电脑软件/scripts/build_windows.ps1` 或 `build_android.ps1`，它们负责英文临时路径和产物回拷。
`public_release/`、构建缓存和 `.gitignore` 排除的内部项目不是默认源码搜索范围；有具体需要再读取。

用户明确的标签职责：自定义耗材资料、写卡登记、扫码入库、余量与换卷追踪只使用 CUID/FUID。NTAG213 只用于独立设备工作台，不得进入耗材模型、模板或库存接口。不要从旧 NTAG 耗材读写文档恢复被撤除的错误用途；旧账保留供核对。当前功能与实机边界见 [设备标签使用说明](电脑软件/docs/NTAG213设备工作台使用说明.md)。

用户明确的重量规则：每一实物卷固定 1000g，禁止恢复 500g/750g/2kg 等可变单卷容量。当前余量独立保存，严格大于 30g 才能重新装入或继续使用；小于等于 30g 不自动清零。批量库存可以是多卷合计 N×1000g。统一复用 `电脑软件/lib/core/constants/personal_spool_policy.dart` 的常量与判断，前后端、入库、选卷、换卷、同步和回归必须遵守；历史异常余额不得静默截断。

## 验证与交付

- 按受影响行为选测试。文档改动检查引用和内容；代码改动运行相关测试与分析；跨模块变更或发布验收再扩大范围。必要检查通过后，有新改动、失败或未解决疑点才重复。
- 命令、工作目录和专项验收入口见 [开发工作流](docs/development-workflow.md)。需要选择验证范围或排查构建失败时，按需使用 [sohun-validation](.agents/skills/sohun-validation/SKILL.md)。
- 保留用户未提交的工作。交付说明实际改动、验证结果和未验证项；硬件能力以实测为准。
- 正式分发沿用 [发布边界](PUBLIC_RELEASE_MANIFEST.md) 和现有资源许可、签名门禁；本地开发不因此等待发布凭据。
